class Solution {
    public int lengthOfLastWord(String s) {
        int end = s.length()-1;
        while(end>=0 && s.charAt(end)==' '){
            end--;
        }
        int len =0;
        while(end>=0 && s.charAt(end)!=' '){
            len++;
            end--;
        }
        return len;
    }
}
